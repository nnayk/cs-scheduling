  Add first prefs question
    
    Todo:
    1. Change availability endpoint to /preferences
    2. Make a new .js component file for /preferences. String together
       availabilty component + a component for each preference
    3. Each preference question shall have an autosave individually. Once a
       change is detected in user selection show "Saving..." and once the
       backend respond that it successfully updated this preference show
       "Saved" in ui

